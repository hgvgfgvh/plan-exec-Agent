# -----------------------------------------------------------------------------
# WorkSpace — 本机沙盘：MCP 运行时、脚本、下载与生成物不进版本库，仅保留空目录占位
# -----------------------------------------------------------------------------
WorkSpace/*
!WorkSpace/.gitkeep

# -----------------------------------------------------------------------------
# 本地环境与密钥（若需示例文件可改名为 .env.example 并放开跟踪）
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example

# 本机真实配置（含 API Key / Web 密码），仅提交 app.example.yaml
config/app.yaml

# 编辑器/构建临时文件
*.exe~

# -----------------------------------------------------------------------------
# 构建产物与编辑器（按需）
# -----------------------------------------------------------------------------
*.exe
*.dll
*.test
*.out
.idea/
.vscode/

# desktop-cat (.NET)
desktop-cat/**/bin/
desktop-cat/**/obj/
desktop-cat/**/*.user

# 本地发布包（体积大，由 scripts/build-release.ps1 生成）
release/
